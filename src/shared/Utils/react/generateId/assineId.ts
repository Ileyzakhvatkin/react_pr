import { assoc } from "../../js/assoc";
import { generateRandomIndex } from "./generateRandomIndex";
// Генератор случайного индекса xthtp aeyrwb. assoc
export const assineId = assoc('id', generateRandomIndex());
